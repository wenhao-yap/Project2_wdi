<div class = "ten wide column" id="dashboardWrapper">
    <div class="ui info message">
        <i class="close icon"></i>
        <div class="header">Dashboard</div>
        <ul>
            <li>Dashboard in the navigation bar is not working in this page.</li>
            <li>To return to Dashboard, either use the link at footer or by clicking cancel or submit in the form</li>
        </ul>
    </div>
</div>

<div class = "ui grid">
    <div class="ten wide column centered left aligned row">
        <div class="ui segment">
            <div class="ui form">
                <h3 class="centered aligned">Editing: {{tab.name}}</h3>
                <form method="POST" action="/tabs/{{tab.id}}/edit?_method=PUT">
                    <div class="field">
                        <label>Song:</label>
                        <input name="name" type="text" value="{{tab.name}}"/>
                    </div>
                    <div class="field">
                        <label>Composer:</label>
                        <input name="composer" type="text" value="{{tab.composer}}"/>
                    </div>
                    <div class="field">
                        <label>Arranger:</label>
                        <input name="arranger" type="text" value="{{tab.arranger}}"/>
                    </div>
                    <div class="field">
                        <label>Image URL:</label>
                        <textarea rows="2" name="image">{{tab.image}}</textarea>
                    </div>
                    <div class="field">
                        <label>Lyrics:</label>
                        <textarea name="lyrics">{{tab.lyrics}}</textarea>
                    </div>
                    <div class="field">
                        <label>Description:</label>
                        <textarea rows="3" name="description">{{tab.description}}</textarea>
                    </div>
                    <div class="field">
                        <label>Tab Download:</label>
                        <input name="link" type="text" value="{{tab.link}}"/>
                    </div>
                    <div class="field">
                        <label>Youtube:</label>
                        <div class="ui labeled input">
                            <div class="ui label">https://www.youtube.com/watch?v=</div>
                            <input name="youtube" type="text" value="{{tab.youtube}}"/>
                        </div>
                    </div>
                    <div class="field">
                        <input name="submit" type="submit" value="Save Changes" />
                        <button><a href="/dashboard">Cancel</a></button>
                    </div>
                </form>
            </div>
        </div>
    </div>
<div>
